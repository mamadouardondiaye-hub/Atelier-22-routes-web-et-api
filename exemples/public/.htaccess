RewriteEngine On

# Laisser passer les fichiers et dossiers réels (js/, css/, …)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Tout le reste passe par le front-controller
RewriteRule ^ index.php [L]
