RewriteEngine On

# Laisser passer les vrais fichiers/dossiers (js/, css/...) sans les router
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Tout le reste passe par le front-controller
RewriteRule ^ index.php [L]
